# requirements.txt 核心依赖
asyncio
aiohttp        # 异步 HTTP 请求
websockets     # WebSocket 支持
edge-tts       # Edge TTS（推荐，免费）
pyaudio        # 音频播放
anthropic      # Claude API（如果使用）
openai         # OpenAI API（如果使用）
numpy          # 音频处理
pydub          # 音频格式转换
