# 项目开发进度报告

## ✅ 已完成 (Completed)

### 1. 项目基础架构 (Infrastructure)
- [x] **Git 仓库初始化**：已上传至 [GitHub](https://github.com/CGW516/AIMI-AI-DEMO001)。
- [x] **目录结构重构**：
  - `src/core/`：核心模块（商品库、LLM引擎、TTS引擎、弹幕处理）。
  - `src/utils/`：工具模块（过滤器）。
  - `frontend/`：前端 React 项目。
  - `scripts/`：启动和安装脚本。
- [x] **依赖管理**：创建 `requirements.txt` 和 `frontend/package.json`。

### 2. 后端核心 (Backend Core)
- [x] **FastAPI 服务** (`src/api.py`)：实现了状态查询、配置更新、消息获取等接口。
- [x] **模块化实现**：
  - `ProductDatabase`：支持 JSON 文件加载和 RAG 检索。
  - `LLMEngine`：基础类结构，支持 Prompt 构建。
  - `TTSEngine`：支持 Edge-TTS 和 GPT-SoVITS 的调用结构。
  - `LiveAssistant`：主控逻辑，集成了各模块。

### 3. 前端控制面板 (Frontend Dashboard)
- [x] **React + Vite 架构**：搭建了现代化的前端开发环境。
- [x] **可视化界面**：
  - 实时数据展示（消息数、响应时间）。
  - 消息流滚动显示。
  - 系统配置开关（TTS、RAG）。
  - 商品列表展示。
- [x] **API 集成**：前端已通过 Axios 对接后端 API。

---

## 🚧 待开发/待完善 (To Do)

### 1. 核心功能实装 (Core Implementation)
目前部分核心逻辑仍为**模拟桩代码 (Mock)**，需要接入真实服务：

- [ ] **LLM API 对接** (`src/core/llm_engine.py`)
  - **当前状态**：返回固定模拟回复。
  - **目标**：对接阿里云 DashScope (Qwen) 或 Anthropic API。需实现流式调用 (SSE) 以降低延迟。
  
- [ ] **弹幕真实抓取** (`src/core/barrage_handler.py`)
  - **当前状态**：模拟生成弹幕。
  - **目标**：集成 `dy-barrage-grab` 项目，通过 WebSocket 或日志文件实时获取直播间真实弹幕。

- [ ] **TTS 真实合成** (`src/core/tts_engine.py`)
  - **当前状态**：代码已包含 Edge-TTS 调用逻辑，但需验证音频输出设备。
  - **目标**：确保音频能正确输出到虚拟声卡 (VB-Cable)，以便 OBS 采集。

### 2. 系统集成与优化 (Integration)
- [ ] **音频路由配置**：配置虚拟声卡，实现 "AI说话 -> 虚拟麦克风 -> 直播伴侣/OBS"。
- [ ] **多线程/异步优化**：确保 LLM 生成和 TTS 合成并行处理，减少直播延迟。
- [ ] **异常处理**：增加网络断连、API 超时等情况的重试机制。

### 3. 部署与运维 (Deployment)
- [ ] **Docker 化**：编写 `Dockerfile` 和 `docker-compose.yml` 以便一键部署。
- [ ] **生产环境配置**：区分开发环境 (Dev) 和生产环境 (Prod) 的配置。

---

## 📝 下一步计划 (Next Steps)

1. **修改 `src/core/llm_engine.py`**：移除模拟代码，接入 `config.py` 中配置的阿里云 Qwen 模型。
2. **修改 `src/core/barrage_handler.py`**：实现真实的弹幕监听逻辑。
3. **联调测试**：启动直播伴侣，验证 "弹幕 -> AI 回复 -> 语音 -> 直播间" 的全链路。
